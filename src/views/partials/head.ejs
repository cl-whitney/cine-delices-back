<!DOCTYPE html>
<head>
    <!-- Meta tags -->
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />


    <!-- Tab title -->
    <title>Ciné Déciles</title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <a href="/back-office">Dashboard</a>
            </ul>
        </nav>
        <% if (typeof page !== "undefined" && page !== "connexion") { %>
            <form action="/admin/connexion" method="POST">
                <button type="submit">Déconnexion</button>
                <p>La déconnexion t'es permise maintenant...</p>
            </form>
        <% } %>
    </header>